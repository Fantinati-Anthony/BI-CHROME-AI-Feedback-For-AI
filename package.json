{
  "name": "biaif",
  "version": "0.4.0",
  "private": true,
  "description": "BIAIF — BI Chrome AI Feedback For AI (Chrome MV3 extension).",
  "scripts": {
    "test":          "vitest run",
    "test:watch":    "vitest",
    "test:coverage": "vitest run --coverage",
    "lint":          "eslint . --ext .js",
    "build":         "node scripts/build.mjs",
    "build:watch":   "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "vitest":              "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom":               "^24.0.0",
    "eslint":              "^8.57.0",
    "esbuild":             "^0.21.0",
    "fake-indexeddb":      "^5.0.2",
    "@types/chrome":       "^0.0.268",
    "typescript":          "^5.4.5"
  }
}
